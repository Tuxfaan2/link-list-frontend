server {
    listen 6906;  # Change the port to 6906
    server_name localhost;

    location / {
        root /usr/share/nginx/html;  # Path to your built Vue app
        try_files $uri $uri/ /index.html;  # Handle Vue Router
    }

    error_page 404 /index.html;  # Handle 404 errors
}